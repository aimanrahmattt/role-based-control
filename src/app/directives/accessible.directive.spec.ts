import { AccessibleDirective } from './accessible.directive';

describe('AccessibleDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessibleDirective();
    expect(directive).toBeTruthy();
  });
});
